<odoo>

    <record id="view_move_form_inherit_l10n_bg_saft" model="ir.ui.view">
        <field name="name">account.account.form</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_form"/>
        <field name="arch" type="xml">
            <field name="tag_ids" position="after">
                <field name="l10n_bg_saft_account_code" invisible="company_fiscal_country_code != 'BG'"/>
            </field>
        </field>
    </record>

    <record id="view_account_list_inherit_l10n_bg_saft" model="ir.ui.view">
        <field name="name">account.account.list</field>
        <field name="model">account.account</field>
        <field name="inherit_id" ref="account.view_account_list"/>
        <field name="arch" type="xml">
            <field name="tag_ids" position="after">
                <field name="l10n_bg_saft_account_code" invisible="company_fiscal_country_code != 'BG'" optional="hide"/>
            </field>
        </field>
    </record>

</odoo>
