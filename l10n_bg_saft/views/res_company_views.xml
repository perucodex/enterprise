<odoo>
    <record id="view_company_form" model="ir.ui.view">
        <field name="name">res.company.form.inherit.account</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">

            <page name="general_info" position="after">
                <page name="l10n_bg_saft_infos" string="BG SAF-T Information" invisible="country_code != 'BG'"  id="test">
                    <group>
                        <group>
                            <field name="l10n_bg_saft_tax_accounting_basis" invisible="country_code != 'BG'"/>
                            <field name="l10n_bg_saft_tax_entity_type" invisible="country_code != 'BG'"/>
                            <field name="l10n_bg_saft_ownership_structure" invisible="country_code != 'BG'"/>
                            <field name="l10n_bg_saft_ultimate_owner_ids" invisible="country_code != 'BG' or l10n_bg_saft_ownership_structure not in ('3','4')" widget="many2many_tags"/>
                            <field name="l10n_bg_saft_beneficial_owner_ids" invisible="country_code != 'BG'" widget="many2many_tags"/>
                            <field name="l10n_bg_saft_related_partner_ids" invisible="country_code != 'BG'" domain="[('is_company','=','True')]" widget="many2many_tags"/>
                        </group>
                    </group>
                </page>
            </page>

        </field>
    </record>
</odoo>
