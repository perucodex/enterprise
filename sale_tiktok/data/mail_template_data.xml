<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <record id="order_sync_failure" model="mail.template">
        <field name="name">TikTok: Order Synchronization Failure</field>
        <field name="model_id" ref="base.model_res_users"/>
        <field name="subject">Synchronization of TikTok Shop order {{ ctx.get('tiktok_order_ref') }} has failed</field>
        <field name="email_from">{{ (object.company_id.email or object.user_id.email_formatted or user.email_formatted) }}</field>
        <field name="email_to">{{ ctx.get('email_to') }}</field>
        <field name="lang">{{ object.lang }}</field>
        <field name="body_html" type="html">
            <div>
                <p>
                    The synchronization of the TikTok Shop order with reference
                    <t t-out="ctx.get('tiktok_order_ref') or ''">REF</t>
                    encountered an error and was not completed.
                </p>
                <p>
                    Unless the order is canceled in TikTok Shop, no other synchronization will be attempted.
                </p>
                <p>
                    To schedule a new synchronization attempt, proceed as follows:
                    <ol>
                        <li>Enter the Developer Tools.</li>
                        <li>Open the form of the TikTok Shop on which the order was placed.</li>
                        <li>Navigate to the Order Follow-up tab.</li>
                        <li>
                            Set a date for <em>Last Order Synchronization Date</em> that is before the last status update of the order.
                        </li>
                        <li>Save the changes and click on the <em>SYNC ORDERS</em> button.</li>
                    </ol>
                </p>
                <p>
                    If the problem persists, contact
                    <a href="https://www.odoo.com/help/">Odoo support</a>.
                </p>
            </div>
        </field>
    </record>

    <record id="inventory_sync_failure" model="mail.template">
        <field name="name">TikTok: Inventory Synchronization Failure</field>
        <field name="model_id" ref="base.model_res_users"/>
        <field name="subject">Synchronization of TikTok Shop inventory has failed</field>
        <field name="email_from">{{ (object.company_id.email or object.user_id.email_formatted or user.email_formatted) }}</field>
        <field name="email_to">{{ ctx.get('email_to') }}</field>
        <field name="lang">{{ object.lang }}</field>
        <field name="body_html" type="html">
            <style>
                .tiktok_table {
                    width: 100%;
                    max-width: 900px;
                    border-collapse: collapse;
                }

                .tiktok_table th,
                .tiktok_table td {
                    border: 1px solid #888;
                }

                .tiktok_table th {
                    white-space: nowrap;
                }

                .tiktok_table thead th {
                    padding: 0.5em 0.5em;
                    text-align: center;
                    border-bottom: 1px solid #888;
                }

                .tiktok_table tbody th,
                .tiktok_table tbody td {
                    padding: 0.5em 0.5em 1em;
                    vertical-align: top;
                }
            </style>
            <div>
                <p>
                    The synchronization of the inventory for TikTok Shop
                    <b><t t-out="ctx.get('tiktok_shop') or ''">REF</t></b>
                    encountered some errors and was not completed.
                </p>
                <p>Here are the errors sent by TikTok Shop:</p>
                <table class="tiktok_table">
                    <thead>
                        <tr>
                            <th>Product Internal Reference(s)</th>
                            <th>Message</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr t-foreach="ctx.get('error_messages')" t-as="error">
                            <th>
                                <t t-out="', '.join(error['default_codes'])"/>
                            </th>
                            <td>
                                <t t-if="error['message']" t-out="error['message']"/>
                                <t t-else="">
                                    An unknown error occurred during the synchronisation with TikTok Shop.
                                    Unfortunately you need to manually check if the available quantity for the product is correct or not.
                                    If the problem persists, contact
                                    <a href="https://www.odoo.com/help/">Odoo support</a>.
                                </t>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </field>
    </record>

</odoo>
